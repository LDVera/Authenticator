  <div>
    <h2> <%= @photo.title %> </h2>
    <%= image_tag @photo.image_url %>
    <div>
      <%= link_to "editar", "/photos/#{@photo.id}/edit" %>
      <%= link_to "eliminar", @photo, method: :delete %>
    </div>
  </div>